class Solution:
    def reductionOperations(self, nums: List[int]) -> int:
        mini = min(nums)
        nums.sort()
        steps = 0
        res = 0
        curr = mini
        for num in nums:
            if num > curr:
                steps += 1
                curr = num
            res += steps
        return res

