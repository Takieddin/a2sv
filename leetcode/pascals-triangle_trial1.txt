class Solution:
    def generate(self, numRows: int) -> List[List[int]]:
        res = []
        for r in range(numRows):
            row = [1] * (r + 1)
            for i in range(1, r):
                row[i] = res[-1][i] + res[-1][i - 1]
            res.append(row)
        return res

        